version: '3'

services:
  digapp_auth_ms:
    container_name: digapp_auth_ms
    restart: always
    build: .
    ports: 
      - "3000:3000"
    links:
      - digapp_usermanagment_db
  
  digapp_usermanagment_db:
    container_name: digapp_usermanagment_db
    image: mongo
    ports:
      - "27017:27017"